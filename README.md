# servo_control_wep

wep page with 4 sliders to control 4 servo motors 
you can save data from the database or load data 
when submit to ESP is pressed it saves the submited data to it's own database and display it in localhost/angles.php with only the submited angles so the ESP can read it 
